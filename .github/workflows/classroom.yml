name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Pytest_Normalized_Array
      id: pytest_normalized_array
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Pytest_Normalized_Array
        setup-command: pip install numpy pytest
        command: pytest test_exercise.py
        timeout: 10
        max-score: 100
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        PYTEST_NORMALIZED_ARRAY_RESULTS: "${{steps.pytest_normalized_array.outputs.result}}"
      with:
        runners: pytest_normalized_array
